apiVersion: v1
kind: ConfigMap
metadata:
  name: orders-config
data:
  POSTGRES_DB: orders
  POSTGRES_HOST: postgres
  DATABASE_URL: jdbc:postgresql://localhost:5432/orders?useSSL=false&serverTimezone=UTC
